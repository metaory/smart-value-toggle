{
  "name": "value-cycle",
  "displayName": "value-cycle",
  "description": "Cycle or nudge values on the line (at cursor or first match) or in the selection. No default keybindings.",
  "version": "0.0.1",
  "type": "module",
  "publisher": "metaory",
  "icon": ".github/assets/logo.png",
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "cycle",
    "increment",
    "decrement",
    "dial",
    "toggle",
    "value",
    "number",
    "boolean",
    "quote",
    "hex",
    "semver"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/metaory/smart-value-toggle"
  },
  "license": "MIT",
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "title": "value-cycle",
      "properties": {
        "value-cycle.disabledTypes": {
          "type": "array",
          "default": [],
          "description": "Rule types to disable. Types in this list are removed from the effective rules.",
          "items": { "type": "string", "enum": ["boolean", "operatorPair", "quote", "nary", "fraction", "letters", "hexColor", "semver", "constants"] }
        },
        "value-cycle.rules": {
          "type": "array",
          "default": [
            { "type": "boolean" },
            { "type": "operatorPair", "options": { "pair": ["&&", "||"] } },
            { "type": "nary", "options": { "base": 10 } },
            { "type": "fraction", "options": {} },
            { "type": "letters" },
            { "type": "hexColor" },
            { "type": "semver" },
            { "type": "quote" }
          ],
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "type": {
                "type": "string",
                "enum": ["nary", "fraction", "constants", "boolean", "operatorPair", "quote", "letters", "hexColor", "semver"]
              },
              "options": { "type": "object" },
              "when": {
                "type": "object",
                "properties": {
                  "languageId": { "type": "array", "items": { "type": "string" } },
                  "visualOnly": { "type": "boolean" }
                }
              }
            },
            "required": ["type"]
          }
        }
      }
    },
    "commands": [{
      "command": "value-cycle.cycle",
      "title": "Cycle value (increment/decrement)"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.105.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "eslint": "^9.39.2",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
