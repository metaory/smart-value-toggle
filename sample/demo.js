// value-cycle demo: put cursor on a line and use your bound keys (e.g. C-a / C-x)
const enabled = false;
const mode = true && false && 0.2;
const count = 42;
const step = 0.5;
const version = "2.2.3";
const color = "#ff8801";
const letter = "m";
const op = "&&";
